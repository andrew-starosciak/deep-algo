/target
Cargo.lock
**/*.rs.bk
*.pdb
.DS_Store
.env
*.log
/data
/cache

# Docker secrets (NEVER commit)
secrets/
!secrets/.gitkeep

# Docker environment files
.env
.env.local
.env.*.local

# Docker volumes (if using bind mounts)
docker-data/

# AWS latency test state
scripts/.aws-latency-test.state
scripts/.aws-latency-key.pem
