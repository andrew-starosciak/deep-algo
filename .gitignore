/target
Cargo.lock
**/*.rs.bk
*.pdb
.DS_Store
.env
*.log
/data
/cache

# Docker secrets (NEVER commit)
secrets/
!secrets/.gitkeep

# Docker environment files
.env
.env.local
.env.*.local

# Docker volumes (if using bind mounts)
docker-data/

# Runtime state
logs/
.pids/

# AWS/EC2 deploy state
scripts/.*.state
scripts/.*-key.pem

# Claude Code settings (contains API key)
.claude/settings.json
